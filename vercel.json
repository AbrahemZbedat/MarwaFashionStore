{
  "version": 2,
  "builds": [
    { "src": "server.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/products" },
    { "src": "/(.*)", "dest": "server.js" }
  ]
}
