{
  "version": 2,
  "builds": [
    { "src": "server.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/products.json", "dest": "/products.json" },
    { "src": "/(.*)", "dest": "server.js" }
  ]
}
